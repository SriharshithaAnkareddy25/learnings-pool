# Rust
/daemon/target/
**/*.rs.bk

# Python
__pycache__/
*.py[cod]
.venv/
.fastembed_cache/
/mcp/build/
/mcp/dist/
*.egg-info/

# Local node data / secrets (iroh keypair, doc store) — never commit these.
# Unanchored so it matches at any depth (e.g. daemon/data/).
data/
keypair
*.keypair

# Editor / OS
.DS_Store
.idea/
.vscode/

# Stray dir from a hardcoded ${HOME} bug
${HOME}/

# sendme temp blobs
.sendme-send-*/

# venv lib dir
/daemon/lib/
